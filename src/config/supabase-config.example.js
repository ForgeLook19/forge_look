// PLANTILLA: copia este archivo como "supabase-config.js" (mismo folder) y
// rellena tus propios valores. "supabase-config.js" está en .gitignore y
// NUNCA debe subirse al repositorio.
//
// La "anon key" es pública por diseño (se usa desde el navegador) y está
// protegida por las políticas de Row Level Security de cada tabla/bucket,
// no por estar oculta.
export const SUPABASE_CONFIG = {
    url: 'https://TU-PROYECTO.supabase.co',
    anonKey: 'TU-ANON-KEY-PUBLICA'
};
